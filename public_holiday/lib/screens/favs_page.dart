import 'package:flutter/material.dart';

class FavsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("You have no favourites yet - start adding some!")),
    );
  }
}

